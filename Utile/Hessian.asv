close all; clear; clc;

syms x1 x2 x3 x4 p a
f = 3*x1^2 + x2^2 +2+x3^2 + 4*x4^2 + x1*x2 + 2*x1*x4 + 2*x3*x4 - x1 + 8*x2 + 6*x3 +9*x4;
var = [x1, x2, x3, x4]
H = hessian(f, var);
e = eig(H);

Hes = 
for i = 1:length(var)
    for j = 1:length(var)
        
    end
end

H, e